# 2026-05-21 代码审查与缺陷修复迭代

## 迭代目标

学习 BookmarkMind 当前代码，完成一次聚焦核心流程的代码审查，修复证据明确的 bug，并给出后续扩展建议。

## 本次范围

- 建立最小中文项目文档和 harness 说明。
- 审查书签清理、AI 分类和分类应用相关路径。
- 补充最小回归测试脚本。
- 修复本轮审查中确认的缺陷。

## 不做范围

- 不新增复杂 UI 页面。
- 不接入服务端。
- 不新增依赖。
- 不实现完整 AI 多协议网关。

## 计划任务

- [x] 阅读项目结构、入口、核心服务和构建脚本。
- [x] 运行现有类型检查和构建，确认基线状态。
- [x] 补充清理逻辑回归测试。
- [x] 修复确认的清理或整理流程缺陷。
- [x] 运行完整验证。
- [x] 记录完成情况和剩余风险。

## 基线验证记录

- `npm run typecheck`：通过。
- `npm run build`：通过。

## 完成记录

- 建立 `docs/project.md`、`docs/harness.md`、本次 spec、iteration 和 changelog。
- 新增 Node 内置测试脚本，`npm test` 覆盖本轮修复点。
- 修复重复 URL 规范化误判：协议和域名可规范化为小写，但路径不再整体小写，避免把大小写敏感路径误判为重复。
- 修复重复书签清理选择逻辑：重复组默认保留第一条，只选择后续重复项；列表勾选状态和切换逻辑与真实删除 ID 一致。
- 修复 AI 配置自定义模型输入：自定义模型名称输入后仍保留自定义选项，不再因 `model !== 'custom'` 隐藏输入框。
- 修复分类应用对 AI-only 结果的防护：跳过没有 Chrome 书签 `id` 的项，避免向 `chrome.bookmarks.move` 传入 `undefined`。

## 验证记录

- `npm test`：通过，7 个测试全部通过。
- `npm run typecheck`：通过。
- `npm run build`：通过，已生成 `dist/` 扩展产物。

## 剩余风险

- 未在真实 Chrome 扩展环境中加载 `dist/` 进行人工点击验证。
- 死链检测仍使用 `fetch` 的 `no-cors` HEAD 探测，准确性受站点 CORS、网络和扩展 host 权限影响，可能存在误判。
- AI 配置界面展示 Anthropic 和 Gemini 预设，但分类与测试请求仍按 OpenAI 兼容 `/chat/completions` 协议发送，尚未完成原生协议适配。
- 后续处理：死链误判风险和 AI 预设协议不一致问题已在 `docs/iterations/2026-05-21-minimax-ai-risk-fix.md` 中修复；真实 Chrome 扩展端到端验证仍待执行。

## 后续可扩展需求建议

- 增加“移动到回收文件夹/撤销”机制，降低误删书签风险。
- 把死链检测改为可解释结果，区分超时、网络错误、HTTP 状态和 CORS 不可见。
- 为 AI 分类增加供应商适配层，明确 OpenAI 兼容、Anthropic、Gemini 的请求和响应协议。
- 增加分类应用前的冲突提示，例如目标文件夹重名、不可移动书签、部分移动失败统计。
- 接入标准测试框架和浏览器端 E2E 验证，覆盖 popup 真实交互。
